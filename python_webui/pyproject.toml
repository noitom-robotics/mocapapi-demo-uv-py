[project]
name = "pnlink-webui"
version = "1.1.0"
description = "PN-Link MocapApi WebUI demo"
readme = "README.md"
requires-python = ">=3.8,<3.12"
dependencies = [
    "mocap-api",
    "nicegui==2.24.1",
]

[tool.uv]
package = false

[tool.uv.sources]
mocap-api = { path = "../MocapApi", editable = true }
